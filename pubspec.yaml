name: skaner_dc06
description: Skaner owali weterynaryjnych DC06 z Google ML Kit.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9
  google_mlkit_text_recognition: ^0.11.0
  permission_handler: ^11.3.0

flutter:
  uses-material-design: true
